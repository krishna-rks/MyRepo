package interfaceDemo;

public interface Animal {
    void walk();
    void run();
    void eat();

    /*
    // We can not have a concrete method inside an Interface class.
    // We can have only abstract methods.
    public void speak()
    {
        system.out.println("Animals can't talk");
    }
    */
}
